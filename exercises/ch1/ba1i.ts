
    function foobar(pattern: string): string {
        return pattern
    }

    export default function ba1i(params: string[]) {
        const [pattern] = params;
        return foobar(pattern);
    }
        